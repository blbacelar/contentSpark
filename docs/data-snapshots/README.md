# Data snapshots

This folder stores point-in-time exported CSV snapshots for diagnostics and documentation.

Notes:
- These files are not migration inputs.
- Do not treat them as test fixtures unless explicitly copied into tests/fixtures.
- Prefer timestamped filenames for future snapshots.
